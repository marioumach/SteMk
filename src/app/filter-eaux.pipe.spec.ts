import { FilterEauxPipe } from './filter-eaux.pipe';

describe('FilterEauxPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterEauxPipe();
    expect(pipe).toBeTruthy();
  });
});
