import { FilterAutrePipe } from './filter-autre.pipe';

describe('FilterAutrePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterAutrePipe();
    expect(pipe).toBeTruthy();
  });
});
